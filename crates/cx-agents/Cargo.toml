[package]
name = "cx-agents"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# Directly, not through cx-ecs: the `Component` and `Resource` derives expand to
# `bevy_ecs::` paths, so the crate has to be nameable here.
bevy_ecs.workspace = true
cx-core.workspace = true
cx-ecs.workspace = true
cx-module.workspace = true
cx-spatial.workspace = true

[lints]
workspace = true
